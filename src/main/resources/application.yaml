server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shopping_db
    username: root
    password: password
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true